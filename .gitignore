# secrets -- never commit keys, seeds, wallet files or .env
.env
.env.*
!.env.example
*.key
*.pem
wallet*.json
*seed*

# deps and build output
node_modules/
.venv/
venv/
__pycache__/
dist/
build/
target/
.next/
.vite/

.DS_Store

# impeccable: commit surfaces, ignore build output
.impeccable/build/

# wallets: never committed (CLAUDE.md)
keys/
*.keypair.json

# local toolchain (large, reproducible)
.tools/

# local runtime state
state-*.db*
config/localnet.json

# local toolchain and shots
.pw-browsers/
shots/
dist/
node_modules/
